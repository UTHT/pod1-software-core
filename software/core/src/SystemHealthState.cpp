#include "SystemHealthState.hpp"
	

SystemHealthState::SystemHealthState(){
}
SystemHealthState::SystemHealthState(){
		        //Define parametrs and required values to check
}
		

SystemHealthState::~SystemHealthState(){
}
					
States SystemHealthState::syscheck(){
			void.get();//Get the list of parameters from health monitor. 
			//Create a if loop for checking the values.
			//notify the problematic parameter.
     			//create a counter k++
}
States SystemHealthState::run(){
	 						          				//if k=n,Go to SystemHealthCheck
		//state = Arm
    		//else
 		//state = Debug
									          											}
	
